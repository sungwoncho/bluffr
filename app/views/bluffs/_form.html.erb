<%= simple_form_for [@match, @bluff] do |f| %>
  <% if @bluff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bluff.errors.count, "error") %> prohibited this bluff from being saved:</h2>

      <ul>
      <% @bluff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <p class="bubble-2">Did you see that ludicrous display last night?</p>

    <%= f.input :statement_1, placeholder: 'Reply', input_html: { class: "form-control bubble-1", autofocus: true } %>

    <%= f.input :statement_2, placeholder: 'Reply of reply', input_html: { class: "form-control bubble-2" } %><br>

    <p class="bubble-1">Yeah it is true. See you later, mate.</p><br>

    <p class="bubble-2">Mind 'ow you go.</p><br>

    <%= f.submit class: "btn btn-success" %>
  </div>
<% end %>
