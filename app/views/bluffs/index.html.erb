<time><%= @match.match_date %></time>
<%= link_to 'Back to match list', matches_path %>

<%= render partial: 'matches/score_detail', locals: { match: @match } %>

<%= link_to "popular", match_bluffs_path(@match, sort: 'popular') %>
<%= link_to "newest", match_bluffs_path(@match, sort: 'newest') %>

<%= render @bluffs %>

<%= will_paginate @bluffs, renderer: BootstrapPagination::Rails %>

<%= link_to 'New Bluff', new_match_bluff_path(@match) %>
